# Agent Session Manager Requirements
# Python 3.10+

# Vector database for semantic memory
chromadb>=0.4.0

# Local embeddings (no cloud dependency)
sentence-transformers>=2.2.0

# Token counting for context budgets
tiktoken>=0.5.0

# Note: sqlite3 is part of Python standard library (no install needed)
